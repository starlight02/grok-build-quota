# 2026-07-30

- 审查额度与周期实现：Free 的额度上限/剩余量动态取自 `/v1/responses` 的 rate-limit 响应头或 429 `actual/limit` 错误体，但展示周期“日”在服务端和前端写死；付费额度动态取自 `/billing?format=credits` 的 `creditUsagePercent`（Build 分项兜底），当前已按周展示。billing 示例中的 `currentPeriod.type` 未被解析，因此周期并非真正数据驱动。若 Free 改为周重置，数值和耗尽判定大概率仍可用，但会错误显示“日”；若接口字段或错误码随之变化则需同步解析。`cargo test --features ssr` 通过 9/9。
- 完成动态额度、CLI 版本和强制刷新改造：新增 `QuotaPeriod`，合并 responses rate-limit 与 billing 双源并解析日/周/月/滚动周期，未知周期不猜测；套餐识别不再依赖固定 included 金额。新增 `src/check/ssr/version.rs`，从 `https://x.ai/cli/stable`（当前 0.2.114）动态取版本，备用通道/旧缓存/兜底版本降级，1 小时单飞缓存，并写入 Grok CLI 请求头。前端新增独立“强制刷新 Token”开关，有 RT 时检测前刷新；刷新后 auth 写回内存，所有生成新凭据的结果均受未落盘保护。更新 README。验证：13/13 单测、SSR/hydrate check、严格 Clippy、完整 `cargo leptos build --release` 全通过。
